import React from 'react';
import AuthFrom from './AuthForm/AuthForm';

import AuthForm from "./AuthForm/AuthForm";

const Auth = (props) => {
	return (
		<div>
			<AuthForm/>
		</div>
	);
};

export default Auth;